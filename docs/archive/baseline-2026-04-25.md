# Baseline — 2026-04-25 (pre-vendor-flow-stable refactor)

> **Purpose:** Record the exact state of the plugin BEFORE the `feat/vendor-flow-stable` branch makes any change, so we can verify post-refactor that we haven't introduced any NEW regressions.
>
> **Pre-existing failures listed here are tech debt** to be tracked separately; they are NOT blockers for the vendor-flow-stable PR. The merge gate is "no new failures introduced", not "all baseline failures fixed".

## Branch state

- **Source branch:** `1.1.0` at commit `c1ef4da` (`docs(qa): consolidate 1.1.0 QA checklist with FREE/PRO labels`)
- **Working branch:** `feat/vendor-flow-stable`
- **Local site:** `http://wss.local` running, WP 6.9.4, PHP 8.x
- **Pro plugin:** active, branch `1.1.0`, commit `c24ce70`

---

## Code quality baseline

| Tool | Result | Notes |
|---|---|---|
| **PHPCS** (`composer phpcs`) | **239 errors, 883 warnings** | Across 282 source PHP files. Pre-existing tech debt. The `dist/` folder is also being scanned (config issue) — half the violations are duplicates from there. |
| **PHPUnit unit suite** | 7 tests, **1 failure** | `ModelConsistencyTest::test_repositories_use_from_db` — `ExtensionRequestRepository.php` uses deprecated `from_row()` instead of `from_db()`. |
| **PHPUnit integration suite** | 93 tests, **5 failures + 1 error + 1 risky** | Pre-existing failures in service creation, requirements, model edge cases. |
| **PHPUnit `api` suite** | Missing `tests/API` directory | phpunit.xml.dist references a directory that doesn't exist. Pre-existing config error. |
| **PHPUnit `rest` suite** | Missing `tests/Rest` directory | (Same — directory exists but tests inside reference removed test classes per Pro QA results doc.) |
| **PHPStan** | Not run — too noisy on baseline; will run only on changed files post-refactor |

**Merge gate for `feat/vendor-flow-stable`:**
- ❗ PHPCS: post-refactor count for files we touched MUST be ≤ baseline count for those same files
- ❗ PHPUnit unit + integration: post-refactor must produce ≤ baseline failure list (no new failures introduced; we're not on the hook for fixing pre-existing ones)
- ❗ PHPStan on every changed file: 0 new errors

---

## Database integrity baseline

Run via `wp eval` against `wp_wpss_*` tables:

| Metric | Baseline value | Health |
|---|---|---|
| Orphan completed milestones (`status=completed AND vendor_earnings IS NULL`) | **0** | ✅ Healthy |
| Negative wallet balances (`balance_after < 0`) | **0** | ✅ Healthy |
| Published services | 13 | reference |
| Vendor profiles | 3 | reference |
| Total orders | 26 | reference |

**Re-run identical queries after every step** — values for the two health metrics MUST stay at 0. Reference counts may grow if test data is added during testing.

```bash
cd "/Users/varundubey/Local Sites/wss/app/public" && wp eval 'global $wpdb; $r = $wpdb->get_results("SELECT
(SELECT COUNT(*) FROM {$wpdb->prefix}wpss_orders WHERE platform=\"milestone\" AND status=\"completed\" AND vendor_earnings IS NULL) as orphan_completed_milestones,
(SELECT COUNT(*) FROM {$wpdb->prefix}wpss_wallet_transactions WHERE balance_after < 0) as negative_balances,
(SELECT COUNT(*) FROM {$wpdb->posts} WHERE post_type=\"wpss_service\" AND post_status=\"publish\") as published_services,
(SELECT COUNT(*) FROM {$wpdb->prefix}wpss_vendor_profiles) as total_vendors,
(SELECT COUNT(*) FROM {$wpdb->prefix}wpss_orders) as total_orders
"); print_r($r);'
```

---

## Browser flow baseline

All 8 flow screenshots taken during the audit phase, located in `.playwright-mcp/`:

| # | Flow | Screenshot | What's captured |
|---|---|---|---|
| 01 | `/become-a-vendor/` (logged-out) | `vendor-flow-01-become-vendor-loggedout.png` | Hard wall — bare WP login screen (B1) |
| 02 | `/become-a-vendor/` (logged-in customer) | `vendor-flow-02-become-vendor-as-customer.png` | Clean page, single CTA |
| 03 | Dashboard after registration | `vendor-flow-03-dashboard-after-register.png` | Tour modal + duplicate "Create Service" CTAs (F4) |
| 04 | Wizard Step 1 — Basic Info | `vendor-flow-04-wizard-step1.png` | 5 fields, 3 required, character counters |
| 05 | Wizard Step 2 — Pricing | `vendor-flow-05-wizard-step2-pricing.png` | 3 equal-weight pricing tabs (F1), package name+description redundancy (F2), sticky-header bug (B2) |
| 06 | Wizard Step 3 — Gallery | `vendor-flow-06-wizard-step3-gallery.png` | Video URL always visible (F3), sticky-header bug (B2) |
| 07 | Wizard Step 3 — silent validation failure | `vendor-flow-07-wizard-step-after-gallery-skip.png` | Continue clicked without main image — no error toast, no inline error (B3) |
| 08 | Wizard — stepper jump attempt | `vendor-flow-08-wizard-step4-requirements.png` | Stepper appears clickable, but click does nothing (B4) |

**Re-take all 8 screenshots after each refactor step** and visually diff — any unintended regression must be caught here.

---

## Issues being addressed in `feat/vendor-flow-stable`

| ID | Severity | Description | Step |
|---|---|---|---|
| B1 | 🔴 Blocker | Logged-out wall on `/become-a-vendor/` | Step 4 |
| B2 | 🟠 High | Sticky-header rendering bug mid-wizard | Step 5 |
| B3 | 🔴 High | Silent validation failure on wizard | Step 2 |
| B4 | 🟠 Medium | Stepper misleading affordance | Step 2 |
| F1 | 🟠 High | 3 equal-prominence pricing tabs | Step 2 |
| F2 | 🟡 Medium | Package Name + Description redundancy | Step 2 |
| F3 | 🟡 Low | Video URL always visible | Step 2 |
| F4 | 🟡 Medium | Duplicate Create CTAs on dashboard | Step 3 |
| F5 | 🟡 Low | No autosave indicator | Step 2 (via primitive) |
| F7 | 🟠 High | Profile not gated, no marketplace signal | Step 3 + Step 6 |

**Out of scope for this branch (revisit in 1.1.1 or 1.2.0):**
- F6 (onboarding tour timing) — would invalidate Shashank's §16b QA
- 3-tier pricing redesign with delivery defaults — bigger refactor
- PHPCS/PHPStan tech debt cleanup
- Pre-existing PHPUnit failures
- `dist/` folder being scanned by phpcs

---

## Compatibility guarantees (the "no break" contract)

| Surface | Promise |
|---|---|
| **REST API** (`/wpss/v1/*`) | No endpoint signatures or response shapes changed. Payload field names preserved. |
| **AJAX endpoints** (`wpss_*` actions) | Signatures unchanged. `wpss_save_draft`, `wpss_publish_service`, `wpss_become_vendor` keep the same nonce + parameter contract. |
| **DB schema** | No columns dropped or renamed. New columns (if any) are nullable + have defaults. Migration not required. |
| **Hooks + filters** | All existing `do_action`/`apply_filters` keep their name + arg signature. New ones may be added. Pro plugin keeps working without changes. |
| **Templates** | Template paths unchanged (no renames). Theme overrides at `theme/wp-sell-services/...` keep working. |
| **i18n strings** | No removed text-domain strings. POT regenerated at end. |
| **Existing services** | Render with full data. Old "Package Description" values remain stored + viewable; new services simply don't fill the field. |
| **Existing draft services** | Load into the new wizard without data loss. |
| **Gutenberg blocks** | Not touched in this branch. |

---

## Sign-off after baseline capture

- [x] Branch `feat/vendor-flow-stable` created off `1.1.0`
- [x] PHPCS baseline counted (239E / 883W)
- [x] PHPUnit baseline counted (6 failures + 1 error + 1 risky across unit + integration)
- [x] DB integrity queries all return healthy values (0 orphans, 0 negative balances)
- [x] Browser screenshots captured for 8 critical flows
- [x] Compatibility contract documented
