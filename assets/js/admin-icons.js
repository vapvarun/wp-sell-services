/**
 * Lucide icon initialization.
 *
 * @package WPSellServices
 * @since   2.0.0
 */

document.addEventListener( 'DOMContentLoaded', function() {
	if ( window.lucide ) {
		lucide.createIcons();
	}
} );
