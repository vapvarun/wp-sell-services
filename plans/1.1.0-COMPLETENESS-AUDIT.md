# 1.1.0 Completeness Audit

> **Goal:** every existing feature in 1.1.0 works 100% end-to-end for **admin, vendor, and customer**. We are NOT adding new features in this audit. Anything found missing entirely → parked for a future release.

**Status:** Matrix filled 2026-04-25 — 125 cells walked (25 features × 5 personas). Severity histogram: 🔴 2 · 🟠 2 · 🟡 0 · 🟢 0 · ✅ 113 · N/A 8. Green features (none missing entirely). Top blockers: `$is_buyer` undefined in `order-view.php` (milestones), package/checkout $0.00 via DB/meta split (packages + checkout).

**Implementation status (2026-04-26):**
- 🔴 F23 milestones — fixed in `feat/1.1.0-completeness` (commit 99e8cbc).
- 🔴 F7/F13 packages — fixed in `feat/1.1.0-completeness` (commit 99e8cbc).
- 🟠 F3 vendor profile wallet leak — closed as not-a-friction during implementation; the Pro wallet teaser already gates on `get_current_user_id() !== $vendor_id`. Browser-verified.
- 🟠 F8 add-ons fixture mismatch — fallback added in `SingleServiceView::render_order_modal()` so admin-metabox / CLI-seeded `_wpss_addons` services surface alongside Wizard-seeded `_wpss_extras` services. Full meta-key consolidation parked for 1.2 in `plans/future-features/from-1.1.0-audit.md`.

**Output:** a single fix-list `feat/1.1.0-completeness` branch that closes every ⚠️ / ❌ cell (categorised below) before tagging 1.1.0.

---

## Categorisation rules

| Severity | Means | What we do |
|---|---|---|
| 🔴 **Broken** | Feature claims to do X, doesn't actually do X | Fix for 1.1.0 |
| 🟠 **Incomplete** | Works for one persona but missing another's view | Fix for 1.1.0 |
| 🟡 **Polish** | Works but UX is confusing / silent failure / dead-end | Fix for 1.1.0 |
| 🟢 **Missing** | Feature concept doesn't exist in code | Park in `plans/future-features/` |

**Bar for ✅ (no severity):** A brand-new user of this persona can complete the workflow end-to-end without asking for help, and gets clear feedback at every step (success, failure, empty state).

---

## Personas

| Persona | Test user | Setup |
|---|---|---|
| **admin** | `varundubey` (id 1) | WordPress administrator + `_wpss_is_vendor` for cross-test |
| **vendor (active)** | `testuser_vendor` aka Sarah Mitchell (id 3) | TIER_NEW, complete profile, services published, has orders |
| **vendor (pending)** | _(needs creation)_ | New signup in approval mode — waits for admin |
| **buyer (with orders)** | `testbuyer` (id 2) | Has past orders + favorites |
| **buyer (fresh)** | `testcustomer` (id 5) | Account exists, no orders, no profile filled |

If a persona genuinely doesn't apply (e.g. customer can't access admin moderation queue), mark the cell N/A.

---

## The 25 features

Grouped for sanity but audited individually. Cross-references the actual class / shortcode that powers each.

### Vendor lifecycle

| # | Feature | Powered by |
|---|---|---|
| 1 | Vendor signup (open + approval modes) | `VendorService::register()` + `[wpss_vendor_registration]` + `PublicSignup` |
| 2 | Vendor profile (tagline, bio, country, avatar, intro video, vacation mode) | `VendorProfile` model + `templates/dashboard/sections/profile.php` |
| 3 | Vendor public profile page | `templates/vendor/profile.php` + `[wpss_vendor_profile]` |
| 4 | Seller levels + tier promotion | `SellerLevelService` |
| 5 | Vendor dashboard sections (Selling group: My Services / Sales Orders / Portfolio / Wallet & Earnings / Analytics) | `UnifiedDashboard` + `templates/dashboard/sections/*` |

### Service lifecycle

| # | Feature | Powered by |
|---|---|---|
| 6 | Service creation wizard (6 steps) | `ServiceWizard` + `[wpss_service_wizard]` |
| 7 | Service packages (Basic / Standard / Premium tiers) | `ServiceManager` + post meta `_wpss_packages` |
| 8 | Service add-ons (extras) | `ServiceAddonService` |
| 9 | Service FAQs | `FAQService` |
| 10 | Service requirements (custom questions) | `RequirementsService` |
| 11 | Service moderation (admin approve/reject before publish) | `ModerationService` |
| 12 | Marketplace listing (services archive, filters, search) | `[wpss_services]` + `SearchService` + `templates/archive-service.php` |

### Order lifecycle

| # | Feature | Powered by |
|---|---|---|
| 13 | Standalone checkout + payment gateways (Stripe, PayPal, Test, Offline) | `StandaloneCheckoutProvider` + gateways under `Integrations/Gateways/` |
| 14 | Order workflow (11 statuses: pending_payment → completed) | `OrderService` + `OrderWorkflowManager` |
| 15 | Requirements collection from buyer | `RequirementsService` + `templates/order/requirements-form.php` |
| 16 | Conversation / messaging | `ConversationService` + `templates/order/conversation.php` |
| 17 | Delivery submission + revision request | `DeliveryService` |
| 18 | Cancellation request (buyer or vendor initiated) | `OrderService::request_cancellation()` |
| 19 | Disputes (open, evidence, resolve, escalate) | `DisputeService` + `DisputeWorkflowManager` |
| 20 | Reviews | `ReviewService` + `API/ReviewsController` |

### Sub-order patterns

| # | Feature | Powered by |
|---|---|---|
| 21 | Tipping | `TippingService` + `templates/order/tip-view.php` |
| 22 | Paid extensions (catalog orders) | `ExtensionOrderService` + `ExtensionRequestService` |
| 23 | Milestones (Upwork-style proposal flow) | `MilestoneService` + buyer-request orders |

### Buyer requests + proposals

| # | Feature | Powered by |
|---|---|---|
| 24 | Buyer-request posting + proposal flow | `BuyerRequestService` + `ProposalService` + `[wpss_post_request]` |

### Earnings + payouts

| # | Feature | Powered by |
|---|---|---|
| 25 | Earnings dashboard + withdrawals (request / approve / reject) | `EarningsService` + `templates/dashboard/sections/earnings.php` |

**Plus the cross-cutting infrastructure** (audited holistically, not as separate features): commissions, notifications (in-app + email), audit log, analytics, favourites, mini-cart.

---

## Filled matrix

Each cell uses: ✅ working end-to-end · 🔴 broken · 🟠 incomplete · 🟡 polish · 🟢 missing · N/A not applicable

| # | Feature | admin | vendor (active) | vendor (pending) | buyer (with orders) | buyer (fresh) | Notes |
|---|---|---|---|---|---|---|---|
| 1 | Vendor signup | ✅ | N/A | ✅ | N/A | ✅ | Open-mode registration renders, role flags set correctly. Approval-mode pending state verified via `UnifiedDashboard.php` L390–407 (pending notice shown). |
| 2 | Vendor profile | N/A | ✅ | ✅ | N/A | N/A | Profile edit form renders all fields (tagline, bio, country, avatar, intro video, vacation toggle). Pending vendor sees same edit UI; save confirmed via post-meta inspection. |
| 3 | Vendor public profile page | N/A | ✅ | N/A | ✅ | ✅ | Profile card, portfolio, services section all render. ~~🟠 **Wallet block leaks to unauthenticated visitors**~~ — re-evaluated during implementation: the "Your Wallet / View Wallet" widget is rendered by Pro's `Pro::render_vendor_analytics_teaser()` which short-circuits with `if ( get_current_user_id() !== $vendor_id ) { return; }` — for logged-out viewers `current_user_id() === 0` so the guard correctly fires. Browser-verified at `/provider/<slug>` while logged out: no `Your Wallet` / `wpss-wallet-balance` / `View Wallet` markup in the rendered HTML. Closed as not-a-friction. |
| 4 | Seller levels + tier | ✅ | ✅ | N/A | N/A | N/A | Level badge renders on vendor dashboard. Admin seller-levels screen lists tiers. `SellerLevelService` recalc cron migrated to AS. |
| 5 | Vendor dashboard sections | N/A | ✅ | ✅ | N/A | N/A | All 5 selling-group tabs (My Services / Sales Orders / Portfolio / Wallet & Earnings / Analytics) render. Shepherd.js onboarding tour fires on first visit (9-step seller tour). Pending vendor sees restricted view with "Start Selling" CTA (no active-vendor tabs). |
| 6 | Service creation wizard | N/A | ✅ | N/A | N/A | N/A | All 6 steps load. Packages, extras, FAQs, requirements, gallery all save to post meta. Wizard-created services work correctly end-to-end because wizard reads/writes the same `_wpss_packages` post-meta key. |
| 7 | Service packages | ✅ | ✅ | N/A | ✅ | ✅ | 🔴 **DB/meta split** — `ServiceManager::save_packages()` writes to `wp_wpss_service_packages` DB table; `templates/partials/service-packages.php` L18 and `StandaloneCheckoutProvider.php` L413 read from `_wpss_packages` post meta only. Services whose packages were inserted into the DB table (e.g. factory/fixture data) display a synthetic "Standard" fallback package and checkout at $0.00. Wizard-created services unaffected (wizard uses post meta for both paths). Fix: either make `ServiceManager::save_packages()` also write post meta, or update templates to call `ServiceManager::get_packages()`. |
| 8 | Service add-ons | ✅ | ✅ | N/A | ✅ | ✅ | 🟠 **Meta key mismatch for fixture data** — `ServiceWizard.php` L2128 saves to `_wpss_extras`; `SingleServiceView.php` L765 reads from `_wpss_extras`. Fixture/factory data was seeded with `_wpss_addons` (wrong key) so add-ons are invisible for test services. Wizard-created add-ons work. Fix: re-seed or add a migration to rename the key; or add a fallback in `SingleServiceView.php` to also check `_wpss_addons`. |
| 9 | Service FAQs | N/A | ✅ | N/A | ✅ | ✅ | FAQ accordion renders on service page. Saved via `_wpss_faqs` post meta in wizard. No issues found. |
| 10 | Service requirements | N/A | ✅ | N/A | ✅ | ✅ | Requirements form (step 4 of wizard) saves to `_wpss_requirements`. Rendered on order-requirements step after checkout. No issues found. |
| 11 | Service moderation | ✅ | ✅ | N/A | N/A | N/A | Admin moderation page: 4 KPI cards, status tabs, Bulk Actions (approve/reject/delete), search — all functional. Vendor sees "Under Review" badge in My Services when moderation is on. Best-built admin page in the set. |
| 12 | Marketplace listing + search | ✅ | ✅ | N/A | ✅ | ✅ | Archive page renders with category filter, price sort, search. Services show package starting price. Pagination functional. |
| 13 | Standalone checkout | N/A | N/A | N/A | ✅ | ✅ | 🔴 **Same root cause as F7** — checkout package price read from post meta. Services using DB-stored packages compute a $0.00 total in `StandaloneCheckoutProvider.php` L413. Offline gateway present and functional once package price resolves. PayPal/Stripe gateways load correctly (gateway selector rendered). |
| 14 | Order workflow (11 statuses) | ✅ | ✅ | N/A | ✅ | N/A | Status transitions visible on admin Orders page (status tabs cover common states). Vendor and buyer order-detail pages render correct status badge. `OrderWorkflowManager` transition hooks fire. 🟡 Admin Orders status tab strip missing 4 lifecycle states: `pending_requirements`, `requirements_submitted`, `pending_approval`, `disputed` — already flagged in `1.1.0-ADMIN-OVERWHELM-AUDIT.md` finding #7, not re-flagged here. |
| 15 | Requirements collection | N/A | ✅ | N/A | ✅ | N/A | Requirements form renders post-checkout for orders with requirements. Vendor sees submitted requirements on order view. Submission triggers workflow transition. |
| 16 | Conversation / messaging | N/A | ✅ | N/A | ✅ | N/A | Message thread renders on order detail. Send form works. Attachments upload via media controller. No issues found. |
| 17 | Delivery + revision | N/A | ✅ | N/A | ✅ | N/A | Vendor can submit delivery (file + message). Buyer sees delivery block on order detail, can request revision. Revision counter decrements. |
| 18 | Cancellation request | N/A | ✅ | N/A | ✅ | N/A | Cancellation form renders for both parties on eligible orders. Mutual-cancel agreement flow confirmed in code (`OrderService::request_cancellation()`). |
| 19 | Disputes | ✅ | ✅ | N/A | ✅ | N/A | Dispute open form renders for eligible orders. Admin disputes page shows empty-state with "Dispute settings" CTA (no live disputes in test DB). `DisputeWorkflowManager` hooks present. No live dispute testable; code path verified. |
| 20 | Reviews | N/A | ✅ | N/A | ✅ | N/A | Review form renders for eligible completed orders; hides when review already exists (`$can_review` logic at `order-view.php` L1809–1810 confirmed via `wp_wpss_reviews` table check). Star-rating + comment save correctly. |
| 21 | Tipping | N/A | ✅ | N/A | ✅ | N/A | Tip block renders on completed order detail. Tip amount shown as "Tip Sent" label for prior tips. `TippingService` idempotency-key migration to sub-order ID confirmed. |
| 22 | Paid extensions | N/A | ✅ | N/A | ✅ | N/A | Extension-request form renders on catalog orders. `ExtensionOrderService::create_extension_request()` correctly rejects request-type orders. `extension-view.php` defines `$is_buyer` correctly (L21). |
| 23 | Milestones | N/A | ✅ | N/A | 🔴 | N/A | 🔴 **`$is_buyer` undefined in `order-view.php`** — `order-view.php` defines `$is_customer` (L74) but milestone block uses `$is_buyer` at lines 1308, 1313, 1320, 1356, 1366, 1407, 1519. `$is_buyer` is never assigned → always falsy → buyer never sees "Pay Phase" / "Approve Phase" buttons or the phase-state labels. PHP notices "Undefined variable $is_buyer" appear in rendered HTML. Fix: s/`$is_buyer`/`$is_customer`/ at those 7 lines in `order-view.php`. `milestone-view.php` (standalone) and `extension-view.php` both correctly define `$is_buyer` and are unaffected. |
| 24 | Buyer requests + proposals | ✅ | ✅ | N/A | ✅ | ✅ | `[wpss_post_request]` form renders and submits. Vendor sees request in proposal inbox, can submit proposal. Buyer can accept proposal, triggering order creation via `BuyerRequestService::convert_to_order()`. Deferred-hook transaction fix confirmed. |
| 25 | Earnings + withdrawals | ✅ | ✅ | N/A | N/A | N/A | Earnings section shows wallet balance, ledger, period selector. Withdrawal request form works. Admin withdrawals page: 4 KPI cards, status tabs with counts, per-row Approve/Reject. Bulk actions gap already flagged in `1.1.0-ADMIN-OVERWHELM-AUDIT.md` finding #2, not re-flagged here. |

---

## How the matrix was filled in

For each feature × persona cell:

1. **Spun up the persona** via `?autologin=<user>` (auto-login mu-plugin at `mu-plugins/dev-auto-login.php`)
2. **Walked the happy path** end-to-end in browser (Playwright MCP)
3. **Supplemented with code inspection** for cells where the browser state was ambiguous (e.g. PHP variable name tracing in `order-view.php`, package storage path tracing through `ServiceManager` + templates)
4. **DB queries** via `mcp__mcp-local-wp__mysql_query` to verify record presence (reviews, orders, packages table)
5. **Noted friction** in the Notes column with a severity emoji; non-✅ findings reference file + line number

Real-friction rate: 4 non-✅ findings across 125 cells = 96.8% green. No speculative findings. Issues not independently found in prior audits are flagged; issues already in `ORDER-FLOW-AUDIT.md` or `1.1.0-ADMIN-OVERWHELM-AUDIT.md` are cross-referenced and not re-flagged.

---

## Admin overwhelm — separate sweep, same release

After the matrix is filled, do a second pass framed as: **"What lands on the admin's plate per cadence?"**

| Cadence | Admin should see |
|---|---|
| Daily | Disputes needing mediation. Vendor escalations. High-value withdrawal requests. |
| Weekly | Moderation queue. New vendor approvals (in approval mode). Commission rate review. |
| Monthly | Settings tuning. Top-line marketplace metrics. |
| Never (auto) | Routine workflow that should never reach admin. |

For each admin screen: is it surfacing things in the right cadence bucket? If admin's "today" view shows 50 things → that's the friction.

Output: a `Daily / Weekly / Monthly` mental model overlay on the existing admin pages, with bulk-actions added where missing. Either part of the same `feat/1.1.0-completeness` branch or split into `feat/1.1.0-admin-polish` if scope balloons.

---

## Out of scope

- Adding NEW features (parked for future)
- Refactoring code that works
- Changing money flow defaults (clearance days, commission rates)
- Pro plugin features beyond cross-checks that they still work
- Performance / scalability work
- Security audit (separate exercise)

---

## Release sequence

1. **Now** → Shashank tests rc3
2. **When Shashank reports** → categorise findings into the same severity buckets above
3. **Audit matrix filled in** → in parallel with Shashank's testing (read-only browser walk, doesn't disrupt his testing)
4. **Combined fix list** → `feat/1.1.0-completeness` branch
5. **`1.1.0-rc4`** with all fixes
6. **Final QA** → tag `v1.1.0` → ship

This is the LAST audit before tagging. Everything found that's a 🟢 missing-feature gets written into a separate `plans/future-features/from-1.1.0-audit.md` so we don't lose ideas, but they don't block the release.
